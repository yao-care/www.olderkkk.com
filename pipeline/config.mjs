// pipeline/config.mjs
// olderkkk「身體小卡關」自助文系列產線設定（每 3 天 1 篇，離峰自動上線）。
export const MODEL = 'claude-sonnet-5';

export const PATHS = {
  health: 'src/content/health',      // 產出目錄（Astro content collection）
  topics: 'pipeline/topics.tsv',     // 題材佇列（slug \t 困擾 \t 部位提示）
  cursor: 'pipeline/state/cursor.json', // 帳本：下一題索引（隨文章一起 commit）
};

// 自動上線內容的合規閘門（YMYL 保健自助文，無人核准前的防呆）。
export const GUARD = {
  forbidden: ['治療', '根治', '療效', '治好', '痊癒', '醫療行為'], // 療效/醫療宣稱紅線，絕不可出現
  // 服務用語紅線（2026-07-19 站主定案）：主語必須是「運動/肌力訓練」，禁一切徒手療程動詞與服務名＋整脊訊號詞。
  // 身體部位名詞（骨盆/體態/骨架）本身不禁，只要主語是「運動矯正/肌力訓練」即可提（例：「運動矯正改善骨盆前傾」放行；「調理改善骨盆前傾」退回）。
  bannedTerms: ['整骨', '整脊', '整復', '整椎', '正骨', '無痛', '脊椎', '脊柱', '調理', '調整', '端正', '牽引'],
  // 「矯正」特例：只放行「運動矯正…」，落單的矯正（前面不是「運動」）一律退回。
  correctionRule: /(?<!運動)矯正/,
  // 去 AI 味（站主要求）：破折號＝AI 腔最明顯 tell；另補一組近零誤判的強指紋（對齊統一去 AI 味清單），命中即退回。
  // 只收在保健自助文中幾乎不會自然出現的套語/空泛收束/模糊引用；'的發展' 限定 '隨著…的發展'（'隨著年齡增長' 等不誤傷）。
  aiFlavor: /——|值得注意的是|值得一提的是|綜上所述|總而言之|歸根結底|舉足輕重|研究顯示|隨著[^。，]{0,10}的發展/,
  safety: /就醫|就診|醫生|醫師|醫療|專業|評估|檢查/,               // 至少要有就醫/專業安全提醒
  funnel: /\(\/method\)/,                                        // 至少要有導流到招牌頁 /method
};
